# Локальная разработка: скопируйте в .env для запуска через docker-compose.yml
# На продакшене не использовать — там .env.production и docker-compose.prod.yml

DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=kidney_office
