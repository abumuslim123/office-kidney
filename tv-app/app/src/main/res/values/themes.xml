<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompatActivity требует Theme.AppCompat (не android:Theme.Material) -->
    <style name="Theme.App.NoActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="Theme.App.NoActionBar.Fullscreen" parent="Theme.App.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
